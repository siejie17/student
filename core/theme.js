import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#3f6bc4',
        secondary: '#2f528a',
        error: '#f13a59',
    },
};