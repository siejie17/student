import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#6284bf',
        secondary: '#2f528a',
        error: '#f13a59',
    },
};